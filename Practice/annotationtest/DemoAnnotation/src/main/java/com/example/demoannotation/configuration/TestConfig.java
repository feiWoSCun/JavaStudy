/*
package com.example.demoannotation.configuration;

import com.example.demoannotation.annotation.AnnotionTest;
import org.springframework.beans.BeansException;
import org.springframework.beans.factory.config.BeanPostProcessor;
import org.springframework.context.annotation.ComponentScan;
import org.springframework.core.annotation.AnnotationUtils;
import org.springframework.stereotype.Component;
*/

/*
@Component
public class TestConfig implements BeanPostProcessor {
    @Override
    public Object postProcessAfterInitialization(Object bean, String beanName) throws BeansException {
        AnnotionTest annotionTest= AnnotationUtils.findAnnotation(bean.getClass(),AnnotionTest.class);
        return BeanPostProcessor.super.postProcessAfterInitialization(bean, beanName);
    }
}
*/
